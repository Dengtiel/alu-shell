#!/usr/bin/env bash
# This script manages the manage_my_process background process.

PIDFILE="/var/run/my_process.pid"

start() {
    if [ -f "$PIDFILE" ]; then
        return 1  # Indicate that the process is already running
    fi
    
    # Start the manage_my_process in the background
    /path/to/manage_my_process &  # Update this path accordingly
    echo $! > "$PIDFILE"          # Store the PID
}

stop() {
    if [ -f "$PIDFILE" ]; then
        kill "$(cat "$PIDFILE")" && rm -f "$PIDFILE"  # Quote to prevent word splitting
    fi
}

restart() {
    stop > /dev/null 2>&1  # Suppress output from stop
    start > /dev/null 2>&1  # Suppress output from start
    echo "manage_my_process restarted"
}

case "$1" in
    start)
        if start; then
            echo "manage_my_process started"
        else
            echo "manage_my_process is already running."
        fi
        ;;
    stop)
        if stop; then
            echo "manage_my_process stopped"
        else
            echo "manage_my_process is not running."
        fi
        ;;
    restart)
        restart
        ;;
    *)
        echo "Usage: manage_my_process {start|stop|restart}"
        ;;
esac
