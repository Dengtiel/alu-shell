#!/bin/bash
pid=$(pgrep -f "bash ./7-highlander")  # Get the process ID of the full command
if [ -z "$pid" ]; then
    echo "Process 7-highlander not found."
else
    kill -9 $pid  # Forcefully kill the process using SIGKILL
    echo "Killed process 7-highlander (PID: $pid)"
fi
